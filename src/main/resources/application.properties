# Nome da aplicação
spring.application.name=hoffmoney-backend

# Configuração do servidor
server.port=8085

# Configuração do DataSource
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5435/hoffmoney
spring.datasource.username=postgres
spring.datasource.password=oxefood

# Configuração do JPA
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.generate-ddl=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

# Configuração do Swagger
springdoc.api-docs.path=/api-docs

# Dados para envios de email

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hoffmoneyifpe@gmail.com
spring.mail.password=jarq rlwr jmca tkku
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#Erros detalhados
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate=DEBUG